[exhaustive validation]
validate_ttt.py, validate_get_best_move

[random validation]
validate_ttt.py, validate_get_best_move

[constructors]
poc_ttt_provided.py, TTTBoard
    0
    True
    [[" "]]

[types]
class TTTBoard
    _dim int
    _reverse bool
    _board list (list (str))
list (list (int))

[exhaustive domain]
class TTTBoard
    _dim m 
    _reverse any
    _board m (m (["X", "O", " "]))
m (m (0-1))

[random domain]
class TTTBoard
    _dim n
    _reverse any
    _board n (n (["X", "O", " "]))
n (n (0-100))

[variables]
m 1-1
n 4-20

[num random]
0
